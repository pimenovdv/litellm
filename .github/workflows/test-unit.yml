name: "Unit Tests"

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  run-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install uv
          cd Backend
          uv pip install -e . --system
          uv pip install pytest pytest-asyncio pytest-mock pytest-cov respx aiohttp httpx pydantic pyyaml openai python-dotenv fastapi tokenizers tiktoken aioresponses mock --system
      - name: Run tests
        env:
          PYTHONPATH: Backend:./
        run: |
          pytest tests/unit/ -v --cov=Backend/litellm
