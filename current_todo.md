- [ ] Очистить каталог `tests/` от оставшихся не-unit тестов (если они остались).
- [ ] Настроить локальный сбор coverage (`pytest-cov` для бэкенда и `jest` для фронтенда).
- [ ] Выполнить настройку производительности GitHub Actions (кэширование, параллельный запуск, оптимизация `uv` и `npm ci`).
